# Google (Gemini) prompting

- Gemini handles structured markdown well; keep sections short and labeled.
- State role, objective, inputs, and expected output format explicitly.
- For safety-sensitive behavior, use direct imperatives ("Do not …") rather than indirect phrasing.
- If the agent uses tools, align tool descriptions with the system instructions so routing stays consistent.
